total = 0

while True:
    price = int(input())

    if price == 0:
        break
    if price < 0:
        print("ты по-моему перепутал")
        continue
    total = total + price

if total > 1000:
    discount_price = total * 0.9
    print(int(discount_price))
else:
    print(total)
